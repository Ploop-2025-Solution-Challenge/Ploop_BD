spring:
  datasource:
    url: jdbc:mysql://localhost:3307/ploggindb?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: plogger
    password: plogger1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # 개발 중엔 update, 운영 땐 validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  logging:
    level:
      org.hibernate.SQL: debug
